---
layout: docs
sitemap: false
nav: flyctl
---

<% content_for :title do %>
  flyctl <%= locals[:file].strip.gsub(/-/, " ") rescue locals.inspect %>
<% end %>
  
<%= partial locals[:path] rescue locals.inspect %>
  -t, --access-token string   Fly API Access Token
  -j, --json                  json output
      --verbose               verbose output

