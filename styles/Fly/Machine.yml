  # This file tests some combinations of matches to try to avoid errors on proper usage.
extends: substitution

message: "Use '%s' instead of '%s'."

level: suggestion

swap:
  '(?<!local|virtual) machine': Machine
  '(?<!local|virtual) machines': Machines
  '(?<=local|virtual) Machine': machine
  '(?<=local|virtual) Machines': machines
