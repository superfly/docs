```cmd
fly auth login
```

```output
Opening browser to url <u>https://fly.io/app/auth/cli/token</u>
Waiting for session...⣽ 
```

This will open a browser window you can use to register and authenticate with Fly.io.